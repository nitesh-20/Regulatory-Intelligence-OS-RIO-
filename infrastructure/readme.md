# Infrastructure Configurations

This directory stores deployment scripts and configurations:
- Docker and Docker Compose files are located in the root.
- Future Kubernetes Helm charts, AWS CloudFormation templates, and CI/CD pipelines (GitHub Actions) go here.
